@model IEnumerable<GestaoDemanda_Master.Models.StatusDemanda>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StatusNome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StatusDescricao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StatusNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StatusDescricao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StatusDemandaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StatusDemandaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StatusDemandaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
